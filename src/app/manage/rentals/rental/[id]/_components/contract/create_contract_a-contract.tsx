import { Card, CardContent, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";

export default function CreateContractAContract() {
  return (
    <Card>
      <CardHeader/>
      <CardContent>
        
      </CardContent>
      <CardFooter>

      </CardFooter>
    </Card>
  );
};
